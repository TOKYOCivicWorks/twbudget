#budget-tagger
  .input-append
    input.span2(ng-model="tagname",size="13",type="text",placeholder="例: 山田一郎")
    button.btn(type="button",ng-click="addtag()") 関連人物をタグ付けする
  #budget-tags
    span.badge.budget-tag(ng-repeat="tag in tags")
      {{tag}}
